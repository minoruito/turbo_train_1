<% content_for :title, "New sample" %>

<h1>New sample</h1>

<%= form_with(model: @sample, url: confirm_samples_path, method: "POST") do |form| %>
  <%= render partial: "form", locals: {form: form} %>
<% end %>
<br>

<div>
  <%= link_to "Back to samples", samples_path %>
</div>
